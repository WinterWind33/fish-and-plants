# Copyright (C) 2022 Andrea Ballestrazzi

# RPI_GC's header files
set(RPI_GC_HEADER_FILES
    "rpi_gc-config-file.hpp"
)

# RPI_GC's source files
set(RPI_GC_SOURCE_FILES
    "entry-point.cpp"
)

add_executable(rpi_gc ${RPI_GC_HEADER_FILES} ${RPI_GC_SOURCE_FILES})

set_target_properties(rpi_gc
    PROPERTIES
    ARCHIVE_OUTPUT_DIRECTORY_DEBUG ${PRODUCTION_LIB_COMPILATION_OUTPUT_DIR}
    LIBRARY_OUTPUT_DIRECTORY_DEBUG ${PRODUCTION_LIB_COMPILATION_OUTPUT_DIR}
    RUNTIME_OUTPUT_DIRECTORY_DEBUG ${PRODUCTION_EXE_COMPILATION_OUTPUT_DIR}
)
